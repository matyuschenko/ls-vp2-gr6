var db = require('./common/db');
module.exports = function (mail, passwd) {
    // if (req.session.user_id) {
    //     db.users.find({'mail': mail}, function(user) {
    //         if (user) {
    //             req.currentUser = user;
    //             next();
    //         } else {
    //             res.redirect('/sessions/new');
    //         }
    //     });
    // } else {
    //     res.redirect('/');
    // }
    console.log("session");
};